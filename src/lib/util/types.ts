export type buttonStyle = 'primary' | 'secondary' | 'danger';
export type route = {
    iconClass: string
    name: string
    url: string
}

export type sessionSummary = {
    name: string;
    time: string;
    date: string;
};